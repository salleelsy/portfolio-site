import * as React from "react";
import { ResumeSection } from "portfolio-site";

/**
 * ResumeSection carries its own real content (Sallee's resume) — the full
 * paper-on-gray resume page with experience, projects, skills, and education.
 */
export const Default = () => <ResumeSection />;
