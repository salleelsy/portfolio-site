import * as React from "react";
import { VibingSection } from "portfolio-site";

/** VibingSection renders its own real content — shown exactly as the site composes it. */
export const Default = () => <VibingSection />;
