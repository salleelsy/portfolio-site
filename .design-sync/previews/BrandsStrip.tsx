import * as React from "react";
import { BrandsStrip } from "portfolio-site";

/** BrandsStrip renders its own real content — shown exactly as the site composes it. */
export const Default = () => <BrandsStrip />;
