import * as React from "react";
import { AboutCard } from "portfolio-site";

/** AboutCard renders its own real content — shown exactly as the site composes it. */
export const Default = () => <AboutCard />;
