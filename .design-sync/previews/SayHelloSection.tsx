import * as React from "react";
import { SayHelloSection } from "portfolio-site";

/** SayHelloSection renders its own real content — heading, blurb, contact form. */
export const Default = () => <SayHelloSection />;
