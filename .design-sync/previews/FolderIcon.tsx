import * as React from "react";
import { FolderIcon } from "portfolio-site";

/** The macOS-blue folder raster at its natural FolderLink size (72.727px). */
export const Default = () => <FolderIcon className="h-[72.727px] w-[72.727px]" />;
