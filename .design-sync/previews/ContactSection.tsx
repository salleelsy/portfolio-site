import * as React from "react";
import { ContactSection } from "portfolio-site";

/** ContactSection renders its own real content — shown exactly as the site composes it. */
export const Default = () => <ContactSection />;
