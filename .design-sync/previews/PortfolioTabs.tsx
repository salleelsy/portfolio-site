import * as React from "react";
import { PortfolioTabs } from "portfolio-site";

/** PortfolioTabs renders its own real content — shown exactly as the site composes it. */
export const Default = () => <PortfolioTabs />;
