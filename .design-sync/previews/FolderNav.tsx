import * as React from "react";
import { FolderNav } from "portfolio-site";

/** FolderNav renders its own real content — shown exactly as the site composes it. */
export const Default = () => <FolderNav />;
