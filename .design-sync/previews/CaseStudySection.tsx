import * as React from "react";
import { CaseStudySection } from "portfolio-site";

/** CaseStudySection renders its own real content — shown exactly as the site composes it. */
export const Default = () => <CaseStudySection />;
