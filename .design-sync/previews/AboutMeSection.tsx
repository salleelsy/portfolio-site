import * as React from "react";
import { AboutMeSection } from "portfolio-site";

/** AboutMeSection renders its own real content — shown exactly as the site composes it. */
export const Default = () => <AboutMeSection />;
